<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.camping.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.camping.entity.User">
        <id column="user_id" property="userId" javaType="java.lang.Long"/>
        <result column="username" property="username" javaType="java.lang.String"/>
        <result column="password" property="password" javaType="java.lang.String"/>
        <result column="phone" property="phone" javaType="java.lang.String"/>
        <result column="role" property="role" javaType="java.lang.String"/>
        <result column="create_time" property="createTime" javaType="java.time.LocalDateTime"/>
        <result column="update_time" property="updateTime" javaType="java.time.LocalDateTime"/>
    </resultMap>

    <select id="selectByUsername" parameterType="java.lang.String" resultMap="UserResultMap">
        SELECT user_id, username, password, phone, role, create_time, update_time
        FROM users
        WHERE username = #{username}
    </select>

    <select id="selectById" parameterType="java.lang.Long" resultMap="UserResultMap">
        SELECT user_id, username, password, phone, role, create_time, update_time
        FROM users
        WHERE user_id = #{userId}
    </select>

    <insert id="insert" parameterType="com.camping.entity.User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (username, password, phone, role, create_time, update_time)
        VALUES (#{username}, #{password}, #{phone}, #{role}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="com.camping.entity.User">
        UPDATE users
        SET username = #{username},
            password = #{password},
            phone = #{phone},
            role = #{role},
            update_time = #{updateTime}
        WHERE user_id = #{userId}
    </update>

</mapper>
